import React from 'react';
import fakeData from '../fakeData/products.JSON';
import Products from '../ProductsF/Products';


const Shoph = () => {
    const paglu = fakeData.slice(0,20);
    return (
        <div>
            <p>hello bro</p>
        </div>
    );
};

export default Shoph;