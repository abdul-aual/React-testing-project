import React from 'react';

const NoMatch = () => {
    return (
        <div>
            <h1 style={{textAlign:'center'}}>Currently No Friend is Available</h1>
        </div>
    );
};

export default NoMatch;